using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AggroStr : MonoBehaviour
{
    public bool aggroed;

    // Start is called before the first frame update
    void Start()
    {
        aggroed = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
